services:
  - type: web
    name: sodakun-prototype
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PASSWORD
        sync: false  # Render.comのダッシュボードで手動設定
